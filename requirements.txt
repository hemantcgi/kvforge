# KVForge dependencies
# Install: pip install -r requirements.txt
# GPU (EC2): also install torch separately with the CUDA index URL:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121

# ── Vector store & embeddings ─────────────────────────────────────────────────
qdrant-client>=1.9.0
fastembed>=0.3.0

# ── LLM / training ───────────────────────────────────────────────────────────
transformers>=4.40.0
datasets>=2.19.0
peft>=0.10.0
accelerate>=0.29.0
bitsandbytes>=0.43.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# ── Web / API ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0

# ── Document ingestion ────────────────────────────────────────────────────────
pypdf>=4.0.0          # PDF loader (ingestion/pdf_loader.py, pipeline/bedrock_rag.py)
python-docx>=1.1.0    # DOCX loader
pptx>=1.0.0           # PPTX loader (python-pptx on PyPI)
openpyxl>=3.1.0       # XLSX loader
pdfplumber>=0.11.0    # PDF image/table extraction (optional enrichment)
Pillow>=10.0.0        # Image handling (LLaVA loader, PDF images)
requests>=2.31.0      # HTTP-based connectors

# ── Scheduling & background ───────────────────────────────────────────────────
apscheduler>=3.10.0

# ── SSH / remote GPU ─────────────────────────────────────────────────────────
paramiko>=3.4.0

# ── Crypto (remote GPU profile encryption) ───────────────────────────────────
cryptography>=42.0.0

# ── Cloud LLM providers (sleep-time FAQ generation) ──────────────────────────
anthropic>=0.25.0
openai>=1.30.0
# google-generativeai>=0.5.0   # uncomment for Gemini support

# ── Misc utilities ────────────────────────────────────────────────────────────
numpy>=1.26.0
